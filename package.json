{
  "name": "jackdevops",
  "version": "0.1.0",
  "private": true,
  "description": "AI-ready, full-lifecycle DevOps platform: requirement to operations",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
