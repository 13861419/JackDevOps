# JackDevOps server environment
# Copy to .env and adjust for your machine.
# If unset, the server runs with an in-memory event store (non-persistent).
DATABASE_URL=postgresql://jackdevops:jackdevops@localhost:5433/jackdevops
PORT=3000
